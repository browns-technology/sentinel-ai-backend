services:
  - type: web
    name: sentinel-ai-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    env: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
